import DriversPage from "~/components/Entity/Drivers/Pagina";
import React, { FC, ReactElement } from "react";

const drivers: FC = (): ReactElement => {
  return <DriversPage />;
};

export default drivers;
